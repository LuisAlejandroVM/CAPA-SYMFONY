{% extends('base.html.twig') %}

{% block title %}
    Listado de usuarios | Symfony
{% endblock %}

{% block stylesheets %}
    <style>

    </style>
{% endblock %}

{% block body %}
    <div>
        <h1>Hola a todos</h1>
        <a href="{{ path('createUser') }}" class="btn btn-primary">Registrar usuario</a>

        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Correo</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for user in listUsers %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ user.name }}</td>
                        <td>{{ user.lastname }}</td>
                        <td>{{ user.email }}</td>
                        <td>
                            <a href="javascript:void(0);" class="btn btn-primary">Modificar</a>
                            {# <a href="{{ path('updateUser', {'id': user.idUser}) }}" class="btn btn-primary">Modificar</a>
                            <a href="{{ path('deleteUser', {'id': user.idUser}) }}" class="btn btn-delete">Eliminar</a> #}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

{% block javascripts %}
    <script>

    </script>
{% endblock %}